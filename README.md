# About the app
The app is Linux only brightness controller. Doesn't work for other OS. It is like a hobby project.
And it is built by using python(Flask), html, css, js and sqlite3 for the database.

## Run
After you clone the program you can run the app by being in the main Dir with this command
flask --app app run --debug
